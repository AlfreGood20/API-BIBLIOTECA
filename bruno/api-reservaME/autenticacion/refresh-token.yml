info:
  name: refresh-token
  type: http
  seq: 4

http:
  method: POST
  url: "{{URL-API}}/auth/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
