info:
  name: obtener-libro-por-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL-API}}/libros/public/:id"
  params:
    - name: id
      value: "0"
      type: path
      description: Id especifico del libro.
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
